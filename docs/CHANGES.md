# 整理记录

- 仅复制六个核心 Python 模块、初始化文件和两个启动入口。
- 排除原 config_presets.json、内部部署启动参数、截图、报告、数据集和模型输出。
- 移除 LLM.py 中内网默认地址，加入环境变量回退。
- 界面不再从配置文件加载 API Key，保存配置时清空 API Key 字段。
- 修正示例数据为程序实际读取的 generated_prompt 字段。
- 补充 BeautifulSoup 依赖，保留原有评分实现。
- 修复单文件 CLI 在异步函数内调用同步 asyncio.run 包装器的问题。
- 三处默认权重统一为历史报告抽查值，仍支持界面自定义权重。
- 启动脚本定位自身目录并绑定 127.0.0.1。
- MIT 文本是待作者确认的草案；未执行公开发布。

原档案目录不做任何修改。依赖范围是整理建议，尚未形成锁定环境。
